# 기능 요구사항 정의 – AI 요약 노트

## 1. 구현 목표
- 로그인 없이 간단하게 메모를 작성하고, AI(모의) 요약 기능을 통해 내용을 정리할 수 있는 서비스
- 핵심: "노트 CRUD + 요약 생성" 기능을 객체 분리와 책임 중심으로 구현

## 2. 필수 기능 목록 (우선순위 포함)
| 우선순위 | 기능 | 설명 |
|----------|------|------|
| ⭐⭐⭐ | 노트 생성/조회/수정/삭제 | 제목/본문 필수, 예외 처리 포함 |
| ⭐⭐ | 노트 목록 & 검색 | 제목 키워드 검색 + 페이징 |
| ⭐⭐ | 요약 생성(Mock) | 간단한 문자열 가공 → 요약 반환 |
| ⭐ | 에러 처리 | JSON 에러 응답 형식 통일 |
| ⭐ | Swagger 미사용 → README에 API 문서 | (테코스 제출 시 Swagger 필수 아님) |

## 3. 선택 기능 (시간 남으면 도전)
- 요약 스타일 선택(간단/자세히)
- 정렬 조건 추가
- 사용자가 쓴 총 노트 개수 통계

## 4. API 명세 (간단 버전)
| 기능 | METHOD | URL | 요청 바디 | 응답 |
|-------|--------|-----|-----------|-------|
| 노트 생성 | POST | /notes | {title, content} | {id} |
| 노트 목록 | GET | /notes?query= | - | [{id,title,...}] |
| 노트 상세 | GET | /notes/{id} | - | {id,title,content,...} |
| 노트 수정 | PATCH | /notes/{id} | {title?, content?} | 200 |
| 노트 삭제 | DELETE | /notes/{id} | - | 200 |
| 요약 생성 | POST | /notes/{id}/summary | - | {oneLine, paragraph} |

## 5. 예외 상황 정의
- 필수값 누락 → 400
- 존재하지 않는 노트 조회 → 400
- 요약 생성 요청 시 본문 길이가 너무 짧을 경우 → 400

## 6. 설계 방향 (중요)
- **Controller → Service → Repository** 계층 분리
- **DTO / Entity 분리**
- **예외 처리 공통화**
- 요약 기능은 Mock → 나중에 진짜 LLM으로 교체 가능하도록 인터페이스 사용

## 7. 구현 진행 방식
1단계: CRUD → 2단계: 예외/검증 → 3단계: 요약 → 4단계: 리팩토링 → 5단계: 테스트
